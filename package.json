{
  "name": "strix-scrum-poker",
  "main": "main.js",
  "version": "0.10.3",
  "description": "strix scrum poker solution",
  "repository": {
    "type": "git",
    "url": "git@github.com:akaWolv/strix-scrum-poker.git"
  },
  "engines": {
    "node": "10.x"
  },
  "license": "CC-BY-4.0",
  "scripts": {
    "start": "echo port:${PORT} && cd Server && npm start",
    "install:server": "cd Server && npm install",
    "install:webapp": "cd Webapp && npm install",
    "postinstall": "concurrently \"npm run install:server\" \"npm run install:webapp\" "
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
