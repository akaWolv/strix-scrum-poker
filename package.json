{
  "name": "strix-scrum-poker",
  "main": "main.js",
  "version": "0.1.0",
  "description": "strix scrum poker solution",
  "scripts": {
    "webapp": "cd Webapp && npm run install:start",
    "server": "cd Server && npm run install:start",
    "start": "npm build && concurrently \"npm run server\" \"npm run webapp\" "
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
